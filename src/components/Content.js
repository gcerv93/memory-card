import Scores from "./Scores";
import Cards from "./Cards";

const Content = () => {
  return (
    <div className="content-box">
      <Scores />
      <Cards />
    </div>
  );
};

export default Content;
